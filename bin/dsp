#! /usr/bin/env node

var app = require('../lib/app');

require('../lib/monitor');
require('../lib/pd');
require('../lib/client');

app.on('client connection', function(socket){
	app.emit('socket', socket);
});

app.emit('setup');
